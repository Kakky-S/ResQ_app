class Data{
  String key;
  String answer;
  String content;
  String question;
  String nextId;
  DateTime createdTime;

  Data({this.key, this.answer,this.content, this.question, this.nextId,this.createdTime});
}